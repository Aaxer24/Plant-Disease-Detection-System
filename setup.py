"""Setuptools entrypoint. All package metadata lives in pyproject.toml — this
file exists only for tooling (older pip/DVC/CI) that expects it to be present.
"""

from setuptools import setup

setup()
