# Potato-Disease-Detection-System
I built a CNN using the PlantVillage dataset to classify healthy and diseased plant leaves. The model uses convolution and pooling layers to learn visual features, with data augmentation and proper dataset splitting applied during training. It achieved high accuracy in multi-disease classification.
